using System;
using System.Collections.Generic;

	public class renard
	{
	  private Id =  "REN-001"(get,set)
	  public Surnom = "Rox" (get , set)
	  public Age = 8(get,set)
	  public Poids = 5 (get,set)
	  public NiveauScociabilite = 10
	  public EstMalade = "Etat de santé";
	  
	  
		public static void Constrcuteur (Id (string) ,Surnom (string),Age (int) ,Poids (double),NiveauScociabilite (int),EstMalade (bool) :
		{
		  Id = id;
		  Surnom = Surnom;
		  Age = age;
		  Poids = poids;
		  NiveauScociabilite = niveausociabilite;
		  EstMalade = estmalade;
		  
		  default = EstMalade (false)
		
		public static void CalculerIndiceCompatibilite (Renard autreRenard) :
		{
	  	Point = point
	  	
	  	 if (Renard{age}) != autreRenard{age} == (age < 6);
	  	 point + 20
	  	 
	  	 if ({poids}) == (poids > 3);
	  	 point - 15
		  
		  
		  
		}
		
		Console.WriteLine("Renard");
	} 
	 
}
